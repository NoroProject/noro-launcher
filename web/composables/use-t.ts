/**
 * Доступ к переводу из компонента: `const { t } = useT()`.
 *
 * Каталог один на всё приложение и живёт в плагине, поэтому здесь только
 * короткий путь к нему — чтобы в разметке не мелькал `useNuxtApp()`.
 */

export function useT() {
  return useNuxtApp().$i18n
}
