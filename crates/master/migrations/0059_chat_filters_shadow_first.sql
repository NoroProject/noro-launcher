-- Автомодерация выкатывается в режиме наблюдения.
--
-- 0053 засеяла фильтр рекламы сразу в `punish`, а 0057 проставила ему пункт
-- свода 1.4 — у того санкция «мут от 1440 до 4320 минут». Вместе это значит,
-- что первый же игрок со ссылкой в чате получает автоматический мут на сутки,
-- без модератора и до того, как пороги хоть раз видели живой чат.
--
-- План на этот счёт однозначен: сначала `shadow` — сообщения проходят,
-- срабатывания видны стаффу и пишутся в `automod_triggers`. Ложные ловятся до
-- того, как начнут стоить игрокам суток тишины; наказания включаются одной
-- правкой в админке, когда пороги настроены.
--
-- Открытые вопросы плана про доли капса, окно флуда и окно эскалации решаются
-- там же и тогда же — «на живом чате», а не заранее.
UPDATE chat_filters SET mode = 'shadow';
